Day3
1.实现了分页查询的功能，利用pagehelper插件，定义了方法page，操作和web开发相似利用数据库select查询
2.员工账号状态（禁用和启用）本质上通过数据库update操作更新状态栏
3.修改员工操作，分为了两个操作，即点击修改用户时用户信息的回显，和用户信息的修改，两个操作也都对应着数据库的select和update操作，和web开发所用操作相似
4.其中新增的分类管理代码大块和上述功能基本一致
5.公共字段填充!利用AOP和映射相关知识，同时包括自己创建注解（Autofill)并在其对应的切面上创建前置操作，用于直接统一赋值创建时间等实时状态的更新操作，
方便了service层，需要每次都添加实时数据更新操作被归结到了切面统一拦截后处理
